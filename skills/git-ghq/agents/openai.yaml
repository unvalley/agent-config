interface:
  display_name: "Create ghq Repository"
  short_description: "Create GitHub repositories in the local ghq tree"
  default_prompt: "Use $git-ghq to create a private GitHub repository and clone it into my local ghq tree."
