# Third-party skills installed via skills.sh (npx skills).
# Each line: <source> -s <skill> [-s <skill> ...]   (repeat -s for each skill).
# Everything before a '#' is passed verbatim to `npx skills add`; `just third-party`
# appends `-g -a claude-code -a codex -a github-copilot -y` (the agents in use).
# Resolved versions are locked in ~/.agents/.skill-lock.json (tracked by chezmoi).

vercel-labs/agent-skills  -s vercel-react-best-practices -s web-design-guidelines -s vercel-composition-patterns
vercel-labs/agent-browser -s agent-browser -s dogfood -s slack -s electron -s vercel-sandbox
vercel-labs/skills        -s find-skills
emilkowalski/skill        -s emil-design-eng
ben-million/skills        -s budge
jakubkrehel/make-interfaces-feel-better -s make-interfaces-feel-better
freee/freee-mcp           -s freee-api-skill
